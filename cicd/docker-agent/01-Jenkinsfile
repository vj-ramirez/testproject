pipeline {
    agent { 
        docker { 
            image 'docker:latest'
            args '-v /var/run/docker.sock:/var/run/docker.sock -u jenkins:jenkins'
        }
    }
    stages {
        stage ('docker test') {
            steps {
                sh '''
                    docker ps; docker images
                '''
            }
        }
    }
}