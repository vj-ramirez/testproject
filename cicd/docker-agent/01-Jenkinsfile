pipeline {
    agent { 
        docker { 
            image 'docker:latest'
            args '-u root -v /var/run/docker.sock:/var/run/docker.sock'
        }
    }
    stages {
        stage ('docker test') {
            steps {
                sh '''
                    docker ps; docker images
                '''
            }
        }
    }
}